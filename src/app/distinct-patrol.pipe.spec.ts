import { DistinctPatrolPipe } from './distinct-patrol.pipe';

describe('DistinctPatrolPipe', () => {
  it('create an instance', () => {
    const pipe = new DistinctPatrolPipe();
    expect(pipe).toBeTruthy();
  });
});
