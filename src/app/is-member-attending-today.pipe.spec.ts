import { IsMemberAttendingTodayPipe } from './is-member-attending-today.pipe';

describe('IsMemberAttendingTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsMemberAttendingTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
